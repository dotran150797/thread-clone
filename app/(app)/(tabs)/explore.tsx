import React from 'react';

import Layout from '@/components/layout';
import ThText from '@/components/text';

const Setting = () => {
  return (
    <Layout>
      <ThText>Setting</ThText>
    </Layout>
  );
};

export default Setting;
